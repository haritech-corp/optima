@extends('layouts.app')
@section('title','Follow-up Center | OPTIMA')
@section('content')
<div class="topbar"><div><div class="eyebrow">Tindakan</div><h1 class="title">Follow-up Center</h1><div class="subtitle">Pantau tindak lanjut hari ini, mendatang, dan overdue.</div></div></div><div class="table-wrap"><table><thead><tr><th>Follow-up</th><th>Lead</th><th>PIC</th><th>Due</th><th>Prioritas</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($followUps as $item)<tr><td><strong>{{ $item->title }}</strong></td><td><a href="{{ route('leads.show',$item->lead) }}">{{ $item->lead?->name }}</a></td><td>{{ $item->assignee?->name }}</td><td class="{{ $item->status==='open' && $item->due_at->isPast() ? 'badge overdue' : '' }}">{{ $item->due_at->format('d M Y H:i') }}</td><td>{{ ucfirst($item->priority) }}</td><td><span class="badge">{{ ucfirst($item->status) }}</span></td><td>@if($item->status==='open')<form method="post" action="{{ route('follow-ups.update',$item) }}">@csrf @method('PATCH')<input type="hidden" name="status" value="completed"><button class="btn small">Selesai</button></form>@endif</td></tr>@empty<tr><td colspan="7" class="empty">Belum ada follow-up.</td></tr>@endforelse</tbody></table></div>{{ $followUps->links() }}
@endsection
